# TomCodeX Dashboard Sync — Environment Variables
# Copy this to .env and fill in your values

# Sync token for authenticating push requests (keep secret!)
SYNC_TOKEN=your-secret-token-here

# Server port (default: 3000)
PORT=3000
